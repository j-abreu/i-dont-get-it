# Keep deterministic for key-free local development, or change to openai.
EXPLANATION_PROVIDER=deterministic

# Required only when EXPLANATION_PROVIDER=openai.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-nano

# Optional server binding overrides.
API_HOST=127.0.0.1
API_PORT=8787
