# API

This directory is reserved for the future server that will validate explanation requests, protect provider credentials, enforce usage controls, and communicate with LLM providers.

The API runtime and deployment target have not been selected. Add a package here only when backend implementation begins.

