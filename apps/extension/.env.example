# WXT loads the committed .env.development and .env.production files by mode.
# Override locally only when testing another API origin. Production values must use HTTPS.
WXT_API_BASE_URL=https://staging-api.example.com
